<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="movieDAO">
	<select id="getAllMovieList" resultType="movie">
		<![CDATA[
			SELECT * FROM MOVIE_DUP ORDER BY POPULARITY DESC
		]]>	
	</select>
	<insert id="insertComment">
		<![CDATA[
			INSERT INTO COMMENT(movie_id) VALUES(#{movie_id})
		]]>
	</insert>
	<insert id="insertMovie">
		<![CDATA[
			INSERT IGNORE INTO MOVIE_DUP(movie_id, title, backdrop_path, genre_ids, original_title,
			release_year, original_language, runtime, poster_path, vote_average, vote_count,
			tagline, overview, popularity, groupNum)
			VALUES(#{movie_id}, #{title}, #{backdrop_path}, #{genre_ids}, #{original_title},
			#{release_year}, #{original_language}, #{runtime}, #{poster_path}, #{vote_average}, 
			#{vote_count}, #{tagline}, #{overview}, #{popularity}, #{groupNum})
		]]>
	</insert>
	<select id="getMovieList" resultType="movie">
		<![CDATA[
			SELECT * FROM MOVIE_DUP ORDER BY GROUPNUM, POPULARITY DESC
		]]>	
	</select>
	<insert id="insertGenre">
		<![CDATA[
			INSERT IGNORE INTO GENRE(genre_id, genre_name)
			VALUES(#{genre_id}, #{genre_name})
		]]>
	</insert>
	<select id="getMovie" resultType="movie">
		<![CDATA[
			SELECT * FROM MOVIE_DUP WHERE movie_id=#{movie_id}
		]]>	
	</select>
	<select id="getSearchMovieTitle" resultType="movie">
		<![CDATA[
			SELECT * FROM MOVIE_DUP WHERE TITLE LIKE CONCAT('%', #{searchKeyword}, '%') ORDER BY POPULARITY DESC
		]]>	
	</select>
</mapper>